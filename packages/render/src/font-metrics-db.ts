/**
 * FontMetricsDB — re-exported from @opendockit/core (single source of truth).
 *
 * The authoritative implementation lives in packages/core/src/font/font-metrics-db.ts.
 * This re-export ensures render always uses the same class and types as core.
 */
export { FontMetricsDB } from '@opendockit/core/font';
export type { FontFaceMetrics, FontMetricsBundle } from '@opendockit/core/font';
