/**
 * @opendockit/pdf/content-stream — PDF content stream builder.
 *
 * Re-exports content stream functionality from @opendockit/pdf-signer/content-stream.
 */

export {
  ContentStreamBuilder,
  formatNumber,
} from '@opendockit/pdf-signer/content-stream';

export type {
  DrawRectOptions,
  DrawLineOptions,
  DrawImageOptions,
  DrawTextOptions,
  DrawTextLinesOptions,
} from '@opendockit/pdf-signer/content-stream';
